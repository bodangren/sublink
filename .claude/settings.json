{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "command": "file=\"$CLAUDE_FILE_PATH\"; if echo \"$file\" | grep -qE '\\.(ts|tsx)$'; then npx eslint --fix \"$file\" 2>/dev/null || true; fi"
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Edit|Write",
        "command": "echo \"$CLAUDE_FILE_PATH\" | grep -qE '(package-lock\\.json|dist/)' && echo 'BLOCK: Do not edit lock files or dist/' && exit 1 || exit 0"
      }
    ]
  }
}
